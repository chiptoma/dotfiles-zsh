# ==============================================================================
# * ZSH LOGIN PROFILE
# ? Runs for login shells only. Keep minimal.
# ? Homebrew: Detected in .zshenv via zsh_detect_homebrew
# ? SSH Agent: Detected in .zshrc via zsh_detect_ssh_agent
# ==============================================================================
